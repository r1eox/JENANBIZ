# دليل إعداد قاعدة بيانات Firebase للتقييمات

## الطريقة 1: استخدام Local Storage (مؤقت - يعمل حالياً)
حالياً النظام يعمل باستخدام Local Storage في المتصفح. التقييمات تُحفظ محلياً على جهاز كل مستخدم.

## الطريقة 2: استخدام Firebase (قاعدة بيانات سحابية عالمية)

### الخطوات:

#### 1. إنشاء مشروع Firebase
1. اذهب إلى: https://console.firebase.google.com
2. اضغط "Add project" أو "إضافة مشروع"
3. اسم المشروع: `jenan-star` (أو أي اسم تريده)
4. اتبع الخطوات حتى يتم إنشاء المشروع

#### 2. تفعيل Realtime Database
1. من القائمة الجانبية، اختر "Realtime Database"
2. اضغط "Create Database"
3. اختر موقع الخادم (مثال: us-central1)
4. اختر "Start in test mode" للبداية
5. اضغط "Enable"

#### 3. إعداد قواعد الأمان
في تبويب "Rules"، استبدل القواعد بالتالي:

```json
{
  "rules": {
    "reviews": {
      ".read": true,
      ".write": true,
      "$reviewId": {
        ".validate": "newData.hasChildren(['name', 'location', 'rating', 'text', 'date', 'timestamp'])"
      }
    }
  }
}
```

#### 4. الحصول على إعدادات المشروع
1. اذهب إلى إعدادات المشروع (الترس بجانب Project Overview)
2. في تبويب "General"، انزل إلى "Your apps"
3. اضغط على أيقونة الويب `</>`
4. سجّل التطبيق باسم "Jenan Star Reviews"
5. انسخ كود `firebaseConfig`

#### 5. تحديث ملف reviews.js
1. افتح الملف: `js/reviews.js`
2. استبدل هذا الجزء:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

بالقيم التي حصلت عليها من Firebase

3. غيّر السطر:
```javascript
const useLocalStorage = true;
```
إلى:
```javascript
const useLocalStorage = false;
```

#### 6. إضافة Firebase SDK إلى reviews.html
افتح `reviews.html` وأضف قبل سطر `<script src="./js/main.js"></script>`:

```html
<!-- Firebase SDK -->
<script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-database-compat.js"></script>
```

## ملاحظات مهمة

### Local Storage vs Firebase

**Local Storage (الحالي):**
✅ يعمل مباشرة بدون إعداد
✅ مجاني 100%
❌ كل مستخدم يرى تقييماته فقط
❌ التقييمات تُحذف عند مسح بيانات المتصفح

**Firebase (موصى به):**
✅ قاعدة بيانات عالمية
✅ كل المستخدمين يرون نفس التقييمات
✅ التقييمات محفوظة بشكل دائم
✅ مجاني حتى 100,000 قراءة/يوم
✅ يعمل من أي جهاز في العالم

### الأمان
- قواعد test mode تسمح للجميع بالكتابة والقراءة
- للأمان الأفضل، يمكن إضافة Captcha أو تسجيل دخول
- يمكن تعديل القواعد لاحقاً لمنع الإساءة

### البدائل الأخرى

1. **Supabase** (مجاني ومفتوح المصدر)
2. **MongoDB Atlas** (قاعدة بيانات NoSQL)
3. **Formspree + Airtable** (لعرض التقييمات كجدول)

## الاستخدام

بعد الإعداد:
1. اذهب إلى صفحة `reviews.html`
2. اضغط "+ أضف تقييمك"
3. املأ البيانات:
   - الاسم
   - الموقع (مثال: الرياض، السعودية)
   - التقييم (نجوم من 1 إلى 5)
   - رأيك في الخدمات
4. اضغط "إرسال التقييم"

التقييم سيظهر فوراً لجميع الزوار من أي مكان في العالم! 🌍

## ربط الصفحة في الموقع

تم إضافة رابط "آراء العملاء" في القائمة العلوية.
يمكنك أيضاً إضافة رابط في `index.html`:

```html
<a href="./reviews.html" class="btn btn-outline">شاهد آراء العملاء</a>
```
