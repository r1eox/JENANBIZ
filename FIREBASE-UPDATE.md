# تحديث قواعد Firebase Firestore

## ⚠️ مهم جداً: يجب تنفيذ هذه الخطوات لكي تشتغل لوحة التحكم والنماذج

## الخطوات:

### 1️⃣ الدخول على Firebase Console:
1. اذهب إلى: https://console.firebase.google.com/
2. اختر مشروع: **jenan-star**

### 2️⃣ تحديث قواعد Firestore:
1. من القائمة الجانبية، اختر **Firestore Database**
2. اضغط على تبويب **Rules** (القواعد)
3. **احذف** جميع القواعد الموجودة
4. **انسخ والصق** المحتوى التالي:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // قواعد التقييمات
    match /reviews/{reviewId} {
      allow read: if true;
      allow create: if request.resource.data.keys().hasAll(['name', 'service', 'rating', 'text', 'createdAt']);
      allow update, delete: if true;
    }
    
    // قواعد الطلبات
    match /inquiries/{inquiryId} {
      allow read: if true;
      allow create: if request.resource.data.keys().hasAll(['name', 'phone', 'service', 'createdAt']);
      allow update, delete: if true;
    }
  }
}
```

5. اضغط على **Publish** (نشر)

### 3️⃣ تفعيل Storage (لرفع الملفات):
1. من القائمة الجانبية، اختر **Storage**
2. إذا لم يكن مفعّل، اضغط **Get Started**
3. اضغط على تبويب **Rules**
4. استبدل القواعد بالتالي:

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /inquiries/{allPaths=**} {
      allow read: if true;
      allow write: if request.resource.size < 10 * 1024 * 1024; // حد أقصى 10MB
    }
  }
}
```

5. اضغط على **Publish**

---

## ✅ بعد التحديث:

1. افتح لوحة التحكم: `admin.html`
2. ستشاهد جميع التقييمات والطلبات
3. النماذج ستشتغل وترسل البيانات مباشرة

---

## 📊 التحقق من البيانات:

### لمشاهدة التقييمات:
1. Firestore Database > Data
2. Collection: `reviews`
3. ستشاهد جميع التقييمات المحفوظة

### لمشاهدة الطلبات:
1. Firestore Database > Data  
2. Collection: `inquiries`
3. ستشاهد جميع طلبات العملاء

---

## 🔐 ملاحظة أمنية:

القواعد الحالية مفتوحة للجميع (للتطوير والاختبار).

**في المستقبل** (بعد الاستقرار):
- نضيف **Firebase Authentication** (تسجيل دخول للأدمن)
- نحدد صلاحيات **الحذف والتعديل** للأدمن فقط
- العملاء يقدرون فقط **يضيفون** تقييمات/طلبات جديدة

---

## ❓ إذا واجهت مشاكل:

1. تأكد إنك نشرت القواعد بزر **Publish**
2. جرب مسح الـ Cache: Ctrl + F5
3. افتح Console (F12) وشوف الأخطاء
4. تأكد إن Firebase Config صحيح في الملفات

---

## 📞 اختبار الإشعارات:

بعد إرسال طلب من أي نموذج:
- راح تشوف الطلب في لوحة التحكم `admin.html`
- راح يطلع رابط واتساب في Console (F12)
- راح ترسل بيانات لـ Firebase مباشرة

✅ **كل شيء جاهز بعد تحديث القواعد!**
