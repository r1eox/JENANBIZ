# تحديث قواعد أمان Firebase

## الخطوة النهائية - تحديث قواعد الأمان

1. اذهب إلى: https://console.firebase.google.com
2. اختر مشروع `jenan-star`
3. من القائمة الجانبية → **Realtime Database**
4. اضغط على تبويب **Rules**
5. احذف القواعد الموجودة والصق هذه القواعد:

```json
{
  "rules": {
    "reviews": {
      ".read": true,
      ".write": true,
      "$reviewId": {
        ".validate": "newData.hasChildren(['name', 'location', 'rating', 'text', 'date', 'timestamp']) && newData.child('name').isString() && newData.child('location').isString() && newData.child('rating').isNumber() && newData.child('rating').val() >= 1 && newData.child('rating').val() <= 5 && newData.child('text').isString() && newData.child('text').val().length >= 10 && newData.child('text').val().length <= 1000"
      }
    }
  }
}
```

6. اضغط **Publish**

## شرح القواعد:

✅ **`.read: true`** - الجميع يمكنهم قراءة التقييمات
✅ **`.write: true`** - الجميع يمكنهم إضافة تقييمات
✅ **Validation**:
   - الاسم: نص
   - الموقع: نص
   - التقييم: رقم من 1 إلى 5
   - النص: بين 10 و 1000 حرف
   - التاريخ والوقت مطلوبين

## اختبار النظام:

1. افتح الموقع المحلي أو ارفعه على استضافة
2. اذهب إلى صفحة `reviews.html`
3. اضغط "+ أضف تقييمك"
4. املأ النموذج واضغط "إرسال"
5. التقييم سيظهر فوراً!
6. جرّب فتح الصفحة من جهاز آخر - سترى نفس التقييمات! 🌍

## مهم:

قواعد Firebase الحالية صالحة حتى **10 ديسمبر 2025**.
بعدها ستحتاج لتحديث تاريخ الصلاحية في القواعد أو استخدام القواعد الدائمة أعلاه.

---

## ملاحظة للأمان الإضافي (اختياري):

لمنع الإساءة والسبام، يمكنك لاحقاً:
1. إضافة Google reCAPTCHA
2. تحديد عدد التقييمات من نفس IP
3. مراجعة التقييمات قبل النشر (moderation)

لكن للبداية، القواعد الحالية كافية وآمنة.
